# common/config

Spring 설정 클래스 (`@Configuration`).

- `SecurityConfig` — Spring Security 필터체인, Swagger 경로 허용
- `SwaggerConfig` — springdoc OpenAPI 메타 정보 (예정)
- `JpaConfig` — `@EnableJpaAuditing` 등 JPA 공통 설정 (예정)
